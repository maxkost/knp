cmake_minimum_required(VERSION 3.22)
project(MnistExample)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_VERBOSE_MAKEFILE ON)
include_directories( ${OpenCV_INCLUDE_DIRS} )
include_directories ( "/usr/include/eigen3" )
include_directories("third_party/nodesoup/include")
add_subdirectory( "third_party/nodesoup" )
add_executable(MnistExample main.cpp visualize_network.cpp)
target_link_libraries(MnistExample PRIVATE nodesoup)
